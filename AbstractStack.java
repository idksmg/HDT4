abstract public class AbstractStack<E> implements IStack<E> {
    /**
     * Este método indica si el stack esta vacio.
     * @return size = 0;
     */
    public boolean empty(){
        return size()==0;
    }
}
